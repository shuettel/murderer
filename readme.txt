download-data.R 
downloads the murder data and saves it as a file in /data

wrangle-data.R
Wrangles data stored in /data and saves it in /rda

analysis.R
analyses the data from rda and saves a barplot in /figs